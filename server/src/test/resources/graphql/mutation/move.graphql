mutation Move($row: Int!, $column: Int!) {
    move(row: $row, column: $column) {
        isOver
        isReady
        onMove
        winner
        board {
            row
            column
            value
        }
    }
}